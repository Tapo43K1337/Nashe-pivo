# Копія як .env — лише для локальної генерації або для підказки в CI.
# У GitHub: Settings → Secrets and variables → Actions → New repository secret
# Ім'я: NP_ADMIN_PASSWORD   Значення: ваш пароль (наприклад test1337)
NP_ADMIN_PASSWORD=
